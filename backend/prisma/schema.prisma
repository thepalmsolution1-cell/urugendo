// Prisma Schema for Urugendo PostgreSQL Database

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum VerificationStatus {
  PENDING
  VERIFIED
  REJECTED
}

enum Role {
  USER
  PROVIDER
  ADMIN
}

model User {
  id           String     @id @default(uuid())
  email        String     @unique
  name         String
  contactPhone String?
  role         Role       @default(USER)
  providers    Provider[]
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt
}

model Provider {
  id                 String             @id @default(uuid())
  name               String
  category           String
  contactEmail       String
  contactPhone       String
  location           String
  verificationStatus VerificationStatus @default(PENDING)
  userId             String?
  user               User?              @relation(fields: [userId], references: [id])
  experiences        Experience[]
  createdAt          DateTime           @default(now())
  updatedAt          DateTime           @updatedAt
}

model Experience {
  id                 String             @id @default(uuid())
  name               String
  description        String?
  category           String
  location           String
  price              Float
  verificationStatus VerificationStatus @default(PENDING)
  providerId         String?
  provider           Provider?          @relation(fields: [providerId], references: [id])
  
  // Embedding stored as JSON string / float array for local dev (pgvector extension can be activated when installed on Postgres server)
  embedding          String?

  createdAt          DateTime           @default(now())
  updatedAt          DateTime           @updatedAt
}
